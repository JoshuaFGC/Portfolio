void alertar(int x){
    //Esto toma un numero de referencia según un estado
    //Cuando se llama esta funcion lo que haría es alertar que el proceso se está cayendo
    //Podria mostrar una luz roja cuando se trata de un estado en especifico o simplemente avisar de su parada
    //Sera llamada posiblemente durante la funcion de hilacion
}

void detener_proceso_estado(int x){
    //Esto toma un numero de referencia según un estado
    //Cuando se llama esta funcion lo que haría es detener un proceso para que no se caiga o sobrecargue todo el proceso
    //Podria mostrar una luz roja cuando se trata de un estado en especifico o simplemente avisar de su parada
    //Sera llamada posiblemente durante la funcion de hilacion
}

void hacer_tiempo(){
    //Esto lo recibe vacia
    //Cuando se llama esta funcion lo que haría es generar tiempo o hacer lento todo el proceso
    //Sera llamada posiblemente durante la funcion de hilacion
}