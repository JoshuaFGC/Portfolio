#ifndef generador 
#include <string>
#include <iostream>
#include "Estado.h"

using namespace std;

void generarEstados(vector<Estado> &estados){
    //Se extrae info del json de los estados
    //se guarda en el vector estados 
    //se retorna lleno
}

void generar_orden_ordenes(vector<string> &ordenes){
    //Se extrae el orden de las ordenes
    //se retorna lleno
}

#endif